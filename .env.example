# Database connection
# Use this format for your own PostgreSQL server:
# DATABASE_URL="postgresql://username:password@host:port/database?schema=public"
#
# Example for local PostgreSQL:
# DATABASE_URL="postgresql://postgres:postgres@localhost:5432/trullygiveaway?schema=public"
#
# Example for Docker (if using docker-compose.yml):
# DATABASE_URL="postgresql://postgres:postgres@localhost:5432/trullygiveaway?schema=public"

DATABASE_URL="postgresql://postgres:postgres@localhost:5432/trullygiveaway?schema=public"


# Server port (optional, defaults to 3000)
PORT=3000
